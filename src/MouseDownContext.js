import { createContext } from 'react';

// using to let us know if the user is clicking or pressing tab (for styling purposes)
const MouseDownContext = createContext();

export default MouseDownContext;
